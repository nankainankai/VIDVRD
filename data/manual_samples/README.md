# Manual Samples

This directory contains copied manual annotation samples retained for validation and Gold-data construction.

- `add_VidVRD/`: relation annotation samples from the historical meeting folder.
- `traj_vis_7_7/`: trajectory annotation samples from the historical track-labeling folder.

Do not delete these copies until the team confirms which files are still needed for evaluation.
