# ==================== 基础依赖 ====================
dashscope>=1.14.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.9.0
dds-cloudapi-sdk==0.5.3
flask
pycocotools

# ==================== 说明 ====================
# 追踪默认：OC-SORT
# 检测后端可选：Rex-Omni（本地）或 DINO-X（CloudAPI）
# - 如使用 DINO-X：需要 dds-cloudapi-sdk + DINOX_API_TOKEN
# - 如使用 Rex-Omni：需要额外安装 torch/transformers 等（本文件不强制锁定，避免把环境拖重）