# 端到端烟雾测试问题记录

*记录对象：`ILSVRC2015_train_00772005` 单视频完整流程；状态：待解决；记录日期：2026-08-28。*

> 2026-08-28 路线调整：主链路已恢复为锚点帧 OC-SORT，MASA/hybrid 已从运行入口与正式配置中分离；语义节点已改为同一对象对最多 6 个连续窗口批量调用。本文其余内容保留为调整前烟雾测试的历史证据，尚需重新运行验证实际改善幅度。

> 2026-08-28 复测结果：使用 9.67 秒的 `ILSVRC2015_train_00177003` 完成了新链路复测。OC-SORT 稳定保持 horse/person 两个主 ID，Gold 标注帧上的平均轨迹 IoU 为 0.7618；19 个语义窗口合并为 4 次正式 `qwen3.7-plus` 调用，4 次成功、0 次失败、0 次重试。模型正确识别并定向输出 `person ride horse`。单视频官方兼容评测为 mAP 0.0046、R@50 0.1111；低分主要来自预测几乎覆盖全程骑乘，而 Gold 只标出三段短区间，并且系统未补出 `sit_above` 与 `walk_beneath`。因此，旧测试中的 ID 混乱和 33 次串行调用已明显改善，但关系覆盖和时间边界仍是后续效果问题。

---

## 2026-08-28 复测后的新增问题

这次修改解决了三个明确的工程问题：主链路恢复为锚点帧 OC-SORT，语义调用从 19 次降到 4 次批量请求，批量响应中缺失 `action` 字段和主谓宾方向颠倒的问题也通过提示协议修正。复测后流程可以稳定结束，但进一步暴露出以下关系层问题。

### 1. 预测关系时间范围明显长于 Gold

该视频的官方 Gold 只在以下三个区间标注关系：

| 谓词 | Gold 区间 |
|---|---|
| `ride` | 0–29、120–149、255–284 |
| `sit_above` | 0–29、120–149、255–284 |
| `walk_beneath` | 0–29、120–149、255–284 |

最终系统输出了 10 段 `person ride horse`，包括 30–85、158–205 等 Gold 未标注区间。从可视化看，人物实际上几乎全程都在骑马，因此这里同时存在两种因素：

1. 千问按照真实画面判断持续骑乘，而 VidVRD Gold 只提供了三段稀疏关系标注；
2. 每个约 30 帧的窗口先产生局部区间，随后全局关系节点会合并重叠或紧邻、且证据间隔足够小的同类关系。

这不是“6 个窗口放进一次 API 调用”直接造成的。批处理只减少请求次数，每个窗口仍保留独立 `packet_id`、EvidencePacket、候选词和校验结果；即使恢复为逐窗口调用，后面的跨窗口聚合规则仍会执行。批处理最多可能让模型的判断更连续，但本次没有证据表明它是主要原因。

**新的问题定义：** 系统输出的是画面中的持续关系，官方指标要求的是与稀疏标注区间重合的关系实例。当前时间定位粒度和 VidVRD 标注习惯之间存在偏差。

### 2. `sit_above` 与 `walk_beneath` 在候选阶段就被裁掉

本次共生成 19 个语义 EvidencePacket、两个主谓宾方向。候选统计为：

| 谓词 | 进入候选集次数 |
|---|---:|
| `ride` | 38 |
| `sit_above` | 0 |
| `walk_beneath` | 0 |

因此，千问不是“看见但判断错了”，而是根本没有获得输出后两个谓词的权限。当前 validator 会拒绝候选集之外的谓词，提示词也要求模型不能自行创造谓词。

候选路由只保留得分最高的三个关系大类。本视频选中的是 comparative、contact 和 manipulation，所以 manipulation 类的 `ride` 能进入候选；而两个遗漏谓词同时依赖：

- locomotion/posture：`sit` 或 `walk`；
- geometry：`above` 或 `beneath`。

locomotion 和 geometry 没有同时进入前三类后，这两个复合谓词就被判为不 eligible。规则层虽然已经输出 `person above horse` 和 `horse beneath person`，但它只生成原子几何关系，不会继续组合为 `sit_above` 或 `walk_beneath`。

**新的问题定义：** 当前分层路由对原子关系有效，但对“动作＋空间”的 VidVRD 复合谓词召回不足，而且后处理缺少复合关系生成步骤。

### 3. 单视频官方分数不能直接代表视觉理解质量

本次官方兼容结果为：

- mAP：0.0046；
- R@50 / R@100：0.1111；
- Tagging P@10：0.1429；
- 诊断轨迹：2/2 Gold 主轨迹匹配，Gold 标注帧平均 IoU 为 0.7618。

轨迹质量和 `ride` 语义判断已经成立，但时间区间偏长、复合谓词候选缺失，使 9 条 Gold 关系中只有 1 条按官方条件命中。这个结果说明关系层仍有明显问题，但只来自一个简单视频，不能据此估计整个数据集的 mAP。

### 新问题优先级

| 优先级 | 后续事项 | 目的 |
|---|---|---|
| P0 | 每个方向固定保留少量动作＋空间复合谓词候选 | 先解决 `sit_above`、`walk_beneath` 在进入千问前就消失的问题 |
| P0 | 提示千问返回所有独立成立的关系，而不是只返回最显眼的一条 | 支持 `ride`、`sit_above` 等多标签共存 |
| P1 | 按证据连续段输出和聚合语义区间，避免仅因窗口重叠形成过长区间 | 改善时间定位和官方 tube vIoU |
| P1 | 做少量逐窗口与 6 窗口批处理对照 | 验证批处理是否产生轻微的时间连续性偏置，而不是直接取消批处理 |
| P2 | 再选择 5–10 个简单视频复测 | 判断候选遗漏和时间偏长是不是普遍问题 |

当前不建议撤销批量调用。它已经把本视频的语义请求从 19 次压到 4 次，而且窗口级验证仍然独立。下一步应该优先修复复合谓词候选召回，再处理时间区间聚合。

---

## 📋 结论摘要

这次测试证明了流水线在兼容跟踪路线下可以从视频走到结构化关系导出，但同时暴露出两个决定性问题：**质量上，目标身份在跟踪阶段已经混乱，后续关系无法成立；效率上，12 秒视频触发了 33 次串行千问语义调用，单语义阶段耗时 49 分钟。**

因此，本次结果只能说明“工程链路能够结束”，不能说明“系统已经可用”。当前也没有运行官方评测，不能把 65 条导出关系理解成有效效果。

| 项目 | 本次实际情况 |
|---|---|
| 输入视频 | `sample_folder/sample_vidvrd/videos/ILSVRC2015_train_00772005.mp4` |
| 视频规模 | 12.00 秒，348 帧，29 FPS |
| 运行目录 | [`runs/smoke_vidvrd_00772005_20260827`](runs/smoke_vidvrd_00772005_20260827) |
| 代码版本 | `9b0153410c874763a19fafc89e14723bd91b733a` |
| 实际检测器 | Rex-Omni-AWQ，稀疏检测，基础间隔 5 帧 |
| 实际跟踪器 | `sparse_ocsort` 兼容路线 |
| 关系模型 | `qwen3.7-plus` |
| 强复核模型 | `qwen-vl-max` |
| 流程状态 | 成功导出 65 条关系 |
| 官方评测 | 未运行，`evaluation.state = skipped` |

> **范围说明：本次没有验证 MASA / `hybrid_sparse_reid` 的效果。** 原主路线在跟踪节点因当前环境缺少可用的 MASA/MMCV 编译依赖而中止。为了先完成烟雾测试，本次改用运行专属配置 [`main_smoke_compat.json`](runs/smoke_vidvrd_00772005_20260827/main_smoke_compat.json) 恢复执行。

## 🔄 问题传播路径

```mermaid
flowchart LR
    video[12 秒输入视频] --> rex[Rex 稀疏检测]
    rex -->|大框、误分类| tracker[sparse_ocsort]
    tracker -->|猫、猴、路锥合并| track_qc[轨迹质检失败]
    track_qc -->|只告警，仍继续| qwen[33 次串行千问调用]
    qwen --> relations[65 条受污染关系]
    relations --> review[Qwen-VL-Max 强复核]
    review -->|返回 Markdown 包裹 JSON| parser[复核解析失败]
    parser -->|0 条动作生效| export[原样导出]

    classDef danger fill:#fee2e2,stroke:#dc2626,color:#7f1d1d
    classDef warning fill:#fef3c7,stroke:#d97706,color:#78350f
    classDef neutral fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e

    class rex,tracker,parser danger
    class track_qc,qwen,relations,review warning
    class video,export neutral

    accTitle: 本次烟雾测试的问题传播路径
    accDescr: Rex 检测误差进入兼容跟踪器后造成身份融合，轨迹质检虽失败但未阻断语义调用；千问生成受污染关系，强复核结果又因 JSON 解析问题未被应用，最终原样导出。
```

这条链路说明，当前不是某一个模型单独“反应慢”或“不会判断”，而是**感知误差、身份错误、调用粒度和复核失效叠加**。其中上游 ID 错误会让后面的正确推理也无处绑定。

## ⏱️ 性能与调用量

以下时间来自 [`run_manifest.json`](runs/smoke_vidvrd_00772005_20260827/run_manifest.json)，不包含中断后的环境排查等待：

| 阶段 | 开始/前序完成 | 完成 | 约耗时 | 观察 |
|---|---:|---:|---:|---|
| 词表生成 | 19:42:09 | 19:43:46 | 1 分 37 秒 | 一次 Agent 调用 |
| Rex 检测 | 19:43:46 | 19:51:19 | 7 分 33 秒 | 12 秒视频已偏慢 |
| 跟踪、轨迹质检、规则 | 22:26:01 | 22:26:01 | 秒级 | 兼容路线，不是 MASA |
| 千问语义关系 | 22:26:01 | 23:15:16 | 49 分 15 秒 | 主要耗时来源 |
| 合并与全局整理 | 23:15:16 | 23:15:17 | 约 1 秒 | 非瓶颈 |
| Qwen-VL-Max 强复核 | 23:15:17 | 23:19:24 | 4 分 07 秒 | 一次批量复核 |
| 有效阶段合计 | — | — | 约 62 分钟 | 对 12 秒视频不可接受 |

语义节点的 [`run.log`](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/semantic/run.log) 给出了明确统计：

- 共调用 33 次，33 次成功，0 次失败，0 次重试；
- 轨迹被切成 23 个重叠窗口；
- 前 5 个窗口各处理 3 个对象对，后 18 个窗口各处理 1 个对象对，因此总数为 `5 × 3 + 18 × 1 = 33`；
- 平均每次语义调用约 89.5 秒；
- 没有触发补帧调用，所以 49 分钟不是失败重试造成的。

**判断：主要问题是当前按“窗口 × 对象对”串行调用的编排方式，而不是 API key、网络重试或 Agent 校验本身。** 长视频的窗口数和对象对数量都会继续增长，当前方式没有实用扩展性。

## 🚨 P0：必须优先处理的问题

### 1. 目标 ID 被融合，关系主谓宾失去物理意义

官方标注中的核心对象是两条独立轨迹：`domestic_cat` 和 `monkey`。本次兼容跟踪最终只输出 3 条轨迹：

| Track ID | 覆盖帧 | 主体情况 |
|---:|---:|---|
| 1 | 0–70 | `hose` |
| 2 | 0–345 | `traffic_cone` |
| 3 | 0–345 | `domestic_cat` 185 帧、`traffic_cone` 95 帧、`monkey` 66 帧 |

Track 3 不是稳定目标，而是把猫、猴和路锥先后合并到了同一个 ID。轨迹质检报告 [`track_qc/report.json`](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/track_qc/report.json) 已经明确给出：

- `passed = false`；
- 1 条类别漂移轨迹，共发生 2 次类别变化；
- 6 次大幅位置跳变，发生在第 145、160、165、250、255、340 帧；
- `risk_track_ids = [3]`；
- `needs_strong_review = true`。

直接影响是：真实的 `(monkey, play, domestic_cat)` 和 `(monkey, touch, domestic_cat)` 需要两个独立 ID，而当前只有一条混合动物轨迹，正确三元组在结构上已经无法表达。千问即使看懂了画面，也只能把判断绑定到错误的 Track ID。

### 2. 强复核实际没有应用任何修正

Qwen-VL-Max 对 53 条高风险关系做了复核，返回内容中确实包含 `reject_relation`、`change_predicate` 和 `refine_interval` 等动作，但最终：

- `final_actions = []`；
- `applied_actions = []`；
- 复核前后关系数均为 65。

根因位于 [`src/vidvrd_auto/relations/ops.py`](src/vidvrd_auto/relations/ops.py) 第 636 行附近：代码直接对模型文本调用 `json.loads(vl_result.text)`。本次模型返回的是 ```` ```json ... ``` ```` 包裹的 Markdown 代码块，解析异常后又被静默替换为 `{}`，所以模型给出的全部动作被丢弃。

这不是模型效果问题，而是明确的实现缺陷。当前“强复核成功”只代表 API 请求成功，不代表复核结果被系统采用。

### 3. 12 秒视频产生 33 次串行关系请求

当前关系调用的基本单元过细：每个重叠窗口中的每个对象对都独立请求一次模型。即使单次调用再提速 3 倍，语义阶段仍需约 16 分钟，仍不满足实际使用要求。

问题不能只靠限制一个固定总调用数解决，因为长视频确实需要更多证据；真正需要改变的是调用粒度和任务合并方式。现阶段至少应把“33 次请求全部成功但耗时 49 分钟”视为流程编排失败，而不是正常模型延迟。

## ⚠️ P1：影响质量和可用性的问题

### 4. 检测框与类别本身存在明显错误

Rex 在锚点帧上并非只是“少检”，还出现了大框和类别误判。例如第 0 帧的 `domestic_cat` 框覆盖了画面大块区域，同时产生 `traffic_cone` 和 `hose`；在后续部分锚点帧上虽然能分别检测猫和猴，但猴框也出现过度扩张。

这说明轨迹错误不全是插帧造成的。把基础检测间隔从 5 帧改成 3 帧，会把锚点数量从约 70 增加到约 116，计算量增加约 67%，但不会自动修复锚点上的大框、误分类或 ID 关联规则。**“3 帧 1 检”可以作为后续对照项，但不能作为本问题的独立解决方案。**

原始检测结果见 [`detections.jsonl`](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/detect/detections.jsonl)。

### 5. 轨迹质检失败后仍无条件调用关系模型

`track_qc` 已经判断 Track 3 高风险且整体不通过，但流水线仍立即进入规则关系和 33 次千问调用。结果是系统在已知身份不可靠的前提下继续花费最多的时间和 API 额度。

目前轨迹质检更像一份旁路报告，没有真正参与后续控制。它至少需要影响“哪些轨迹对可以进入关系推理、哪些片段需要修复、哪些结果必须降权或暂缓”，否则质检没有节省成本和阻止污染的作用。

### 6. 两层 QC 的 `passed` 含义互相矛盾

上游 [`track_qc/report.json`](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/track_qc/report.json) 为 `passed = false`，下游 [`verify/qc.json`](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/verify/qc.json) 却为 `passed = true`。

后者实际上只表示最终关系没有触发它自己检查的互斥冲突或类别过滤，并不代表轨迹和关系整体可信。字段同名但语义不同，容易让使用者误以为本视频已经通过全链路质检。

### 7. 无关开放词表目标进入全部关系候选

本次 3 条轨迹中有 `hose` 和 `traffic_cone`，而该视频官方核心目标是猫和猴。当前配置关闭了 `category_constraints_enabled`，这些背景或误检目标仍进入关系枚举，并产生大量 `touch`、`next_to`、`left/right` 等关系。

这既增加了千问调用次数，也扩大了假阳性来源。开放世界发现可以保留，但“发现到的所有对象都平等进入所有关系推理”并不合理；闭集评测和开放关系输出也需要分清用途。

### 8. 最终关系在数量上很多，在语义上没有命中核心事件

最终 65 条关系的构成为：

| 来源/谓词 | 数量 |
|---|---:|
| 几何规则来源 | 44 |
| 千问语义来源 | 21 |
| `next_to` | 22 |
| `touch` | 16 |
| `left` / `right` | 8 / 8 |
| `above` / `beneath` | 3 / 3 |
| `taller` / `larger` | 3 / 2 |
| `play` | 0 |

官方标注中的核心动作 `play` 完全没有出现；`touch` 虽然出现 16 次，但主要绑定在错误的 `hose`、`traffic_cone` 和混合 Track 3 之间。由此可见，关系数量不能用作质量指标。

这里只做定性判断：由于猫和猴没有形成独立稳定轨迹，本视频的核心关系在官方“类别三元组 + tube 匹配”下预计无法正确命中。正式数值仍需启用官方评测后才能报告。

### 9. 调整前的 MASA 默认路线在当前环境不可执行

原始 `hybrid_sparse_reid` 路线在跟踪节点报错 `No module named 'masa'`。随后虽已取得官方 MASA 代码，但其 MMDetection/MMCV 编译算子、模型权重和当前 Windows、Python 3.10、Torch 2.7.1 + CUDA 12.8 环境尚未形成可运行组合。

本次没有为了跑通而强行降级或破坏现有环境，而是明确切换到 `sparse_ocsort` 兼容配置。因此：

- 本次结果不能评价 MASA 是否能改善 ID；
- 该实验路线没有完成真实端到端验证；
- 当前已将它移出主链路，后续若单独研究，仍须与 OC-SORT 结果明确区分。

## 🎯 本次质量对照

| 期望 | 实际 | 判断 |
|---|---|---|
| 猫与猴各有稳定 ID | 二者被并入 Track 3，且混入路锥 | 不通过 |
| 能表达 `monkey play domestic_cat` | `play` 为 0，且缺少两个独立主体 ID | 不通过 |
| `touch` 绑定真实动物对 | 多数绑定错误目标/混合轨迹 | 不通过 |
| 轨迹 QC 阻止高风险结果污染 | QC 失败后仍执行全部关系调用 | 不通过 |
| 强复核能够修正结果 | 模型返回动作，但 0 条被解析和应用 | 不通过 |
| 12 秒视频具有可接受周转时间 | 有效阶段约 62 分钟 | 不通过 |
| 能给出正式量化质量 | 官方评测被跳过 | 尚未评测 |
| 调整前主路线可运行 | MASA 路线未跑通，仅兼容路线完成 | 不通过；现已切回 OC-SORT，待复跑 |

## 🖼️ 可视化与证据

- [轨迹与关系叠加视频](runs/smoke_vidvrd_00772005_20260827/visualizations/review_overlay.mp4)：粗彩色框为预测轨迹，细绿色框为官方 GT，可直接观察 Track 3 的身份融合与大框问题。
- [关键帧联系表](runs/smoke_vidvrd_00772005_20260827/visualizations/review_contact_sheet.jpg)：集中查看多个时刻的预测框、GT 框和关系文本。
- [关系时间线](runs/smoke_vidvrd_00772005_20260827/visualizations/relation_timeline.png)：官方 `play/touch` 位于视频后段，预测则主要是错误 ID 之间的几何与接触关系。
- [最终关系 JSON](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/export/relations.json)
- [最终轨迹 JSON](runs/smoke_vidvrd_00772005_20260827/videos/ILSVRC2015_train_00772005/export/trajectories.json)
- [运行清单](runs/smoke_vidvrd_00772005_20260827/run_manifest.json)

## ✅ 后续修复的最低验收条件

这部分只记录验收门，不在本文展开施工方案：

1. 猫和猴必须形成两条独立轨迹，且同一 ID 不再跨 `domestic_cat → traffic_cone → monkey` 漂移。
2. 轨迹质检失败必须真实影响下游调用，不能在已知 ID 不可靠时照常完成全部语义请求。
3. 强复核返回带代码块、纯 JSON 或轻微格式差异时都能被解析；测试中至少有一条动作能够被验证并实际应用。
4. 关系调用不能继续按所有“窗口 × 对象对”逐个串行请求；12 秒视频不得再处于 50 分钟量级。第一阶段可把当前机器与 API 条件下的完整周转目标暂定为 10 分钟以内。
5. 最终结果必须能表达并正确绑定 `monkey play domestic_cat` 与相应 `touch`，而不是只增加关系数量。
6. 重新运行时必须记录真实 tracker 路线；当前正式结果统一标记为 `sparse_ocsort`，其上游核心仍为 OC-SORT。
7. 质量判断最终应接入官方评测；可视化只用于诊断，不能替代 mAP、Recall@K 和 tagging 指标。

## 🧭 建议解决顺序

后续恢复工作时，建议按依赖顺序处理：

1. 修复强复核 JSON 解析和 QC 状态语义，因为这是明确、局部且可验证的代码缺陷。
2. 先解决检测框与 ID 融合，再讨论关系谓词上限；上游身份不正确时，继续优化千问提示词收益很低。
3. 改造关系调用粒度和批处理方式，消除 33 次串行请求造成的主要耗时。
4. MASA 环境与 hybrid 只在独立实验模块中处理，不作为主链路完成条件。
5. 用同一视频复跑并做官方评测及可视化前后对照，再决定是否增加 DINO-X 关键帧或提高 Rex 检测密度。

---

*最后更新：2026-08-28。本文记录本次单视频烟雾测试已经观察到的问题，不代表整个数据集上的统计结论。*
